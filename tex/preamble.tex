% Paper and margins
% \usepackage[a4paper,inner=3cm,outer=1cm, margin=1cm]{geometry}

% Fonts
\usepackage[UTF8]{ctex}
\usepackage[]{opensans}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% Images
\usepackage{graphicx}
\usepackage{float}

% All images should be where they are defined
\makeatletter
\def\fps@figure{H}
\makeatother

% Headers and footers
\pagestyle{plain}

% title
\usepackage{titlesec}
\titleformat{\chapter}[display]
{\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}

% this alters "before" spacing (the second length argument) to 0
\titlespacing*{\chapter}{0pt}{0pt}{20pt}


% reduce code font size
\usepackage{setspace}
\renewenvironment{Shaded} {\begin{snugshade}\footnotesize\singlespace} {\end{snugshade}}

% color inline code
\usepackage{fancyvrb,newverbs,xcolor}

\definecolor{Light}{HTML}{F4F4F4}

\let\oldtexttt\texttt
\renewcommand{\texttt}[1]{
  \colorbox{Light}{\oldtexttt{#1}}
}

% color no highlight code

% \renewcommand{verbatim}[1]{
%   \colorbox{Light}{#1}
% }
